import { makeStyles } from '@material-ui/core/styles';

const btnContainer = makeStyles({
    root:{
        display:"flex",
        justifyContent:'space-between',
        margin:'0.5rem 0'
    }
});

export {btnContainer}